services:
  backend: 
    build: ./backend
    # environment:
    #   - PORT=8000
    env_file:
      - ./backend/.env
    ports:
      - 8000:7000
      
  frontend:
    build: ./frontend
    ports:
     - 5173:5173